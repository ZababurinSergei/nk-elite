export const actions = async (self) => {

    return {
        run: (event) => {
            console.log('#####################################')
        }
    }
}

export default {
    description: "actions for newkind-osc"
}