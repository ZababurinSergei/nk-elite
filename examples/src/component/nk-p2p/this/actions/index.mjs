export const actions = async function(self) {
    return  {
        message: async (event) => { },
        broadcastChannel: async (event) => { },
        messageerror: async (event) => { },
        click: async (event) => { }
    }
}