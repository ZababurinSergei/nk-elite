export const nkGit = function(self, data) {
    return new Promise((resolve, reject) => {
        // const action = data.value.length === 0 ? 'add': 'remove'
        // self.html.control.button.run.classList[action]('disabled')
        //
        // self.config = {
        //     gitDir: `${self.config.gitUser}/${data.value}`
        // }
        //
        // console.log('RUN: ',`${self.config.gitUser}/${data.value}`)
        // navigator.serviceWorker.controller.postMessage({
        //     type:'service',
        //     message: `${self.config.gitUser}/${data.value}`
        // });
        // console.log('dddddddddddddddddddddd',data.value, this, self.html.control.button.run)
        // debugger
        resolve(true)
    })
}