export const actions = (self) => {
    return new Promise(async (resolve, reject) => {
        resolve({
            bus: {
                frame: async (event) => {

                }
            }
        })
    })
}

export default {
    description: "actions for newkind-osc"
}